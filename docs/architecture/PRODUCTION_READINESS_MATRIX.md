# Production Readiness Matrix

Evaluated against actual repository contents this session, not documentation claims. See
[ARCHITECTURE_DRIFT_REGISTER.md](ARCHITECTURE_DRIFT_REGISTER.md) for the evidence-gathering
detail behind the Productionization row group.

| Area | Status | Evidence | Required next action |
| --- | --- | --- | --- |
| **Identity (Managed IdP/OIDC)** | NOT READY | `packages/auth-service/src/identityProviders.ts` implements only `LocalDevIdentityProvider` — no cryptography, dev-only, no real OIDC/SAML client anywhere in the repo. | Build/adopt a managed-IdP adapter conforming to the existing `IdentityProvider` port before any non-dev user population. |
| **Authentication** | READY (for the current dev/synthetic scope) | Server-side sessions, opaque 32-byte bearer tokens stored only as SHA-256 hashes, 8h expiry, audited issue/revoke events; deactivating a user kills live sessions. 8 integration tests passing. | None for current scope; re-evaluate once a real IdP is added. |
| **Authorization / Access control** | PARTIAL | Static role-policy guards exist and are enforced per command (case/evidence/benefits/authorization/prescreen); Operating Assurance uses a distinct per-resource participant-grant model. No per-target-status role mechanism exists (documented gap: `TransitionCase` permits `INTAKE_COORDINATOR`/`ORGANIZATION_ADMIN` for `MEDICAL_TRANSFER_REQUIRED`, not clinician-gated). | Decide whether a finer-grained per-transition role mechanism is needed (owner decision, not evidence of urgency found). |
| **Tenancy** | READY (dev scope) | `organizationId` scoping enforced in every predicate across audited gateways; a record id is never treated as authorization. Zero-residue and cross-tenant-isolation tests pass (`tests/integration/tenant-isolation.test.ts`, part of this session's 755/755). | None for current scope. |
| **Database (PostgreSQL)** | READY (local dev) / UNKNOWN (production provider) | Local `clarity_dev` verified this session after applying 10 pending migrations. Google Cloud SQL for PostgreSQL is the recorded provider choice, but no authenticated GCP account/project has ever been available for provider-backed execution (`IMPLEMENTATION_STATUS.md`, longstanding). | Provider-backed Cloud SQL verification remains the separate, non-waived gate — needs `gcloud auth login` + a target project. |
| **Row-level security (RLS)** | READY (proven pattern, dev scope) | Transaction-local RLS context + additive direct-tenant policies implemented and verified against local `clarity_dev` for episode, prescreen, and RevOps tables, including a dedicated `NOSUPERUSER NOBYPASSRLS` runtime-role proof. This session found the shared local runtime role (`revops_rls_test`) missing a schema-level `USAGE` grant — an environment-specific gap, not a design flaw (fixed for this session; see DRIFT-01). | Provider-backed RLS evidence (Cloud SQL) remains gated on the same GCP-access blocker as above. |
| **Migrations** | READY, with a known operational hazard | 24 migrations in this worktree, all applied cleanly this session; `prisma validate` clean. The shared-local-database model across git worktrees causes real ledger divergence (DRIFT-01) — this session found 2 foreign migrations present locally and 10 of this worktree's own migrations absent before applying them. | No code fix needed; this is an operational/process hazard (issue #31) inherent to the shared-dev-DB pattern, worth a standing runbook note for future sessions. |
| **API** | PARTIAL | Fastify server; RevOps/IOP/Operating Assurance use native route registration; the original 4 routes plus all 7 prescreen routes still run through a manual `app.all("/*")` catch-all (DRIFT-06). Tenant/actor derivation is exclusively principal-based; unknown fields rejected. | Finish the native-Fastify migration (owner-ruled, scoped in ARCHITECTURE_IMPLEMENTATION_PLAN.md). |
| **Documents (storage)** | NOT READY for real files | Only `InMemoryDocumentStorage` and `LocalFilesystemObjectStorage` exist; the latter's own comment says "DEVELOPMENT ONLY — not a production object store." No S3/Blob adapter exists, stubbed or otherwise. | Build a production object-storage adapter before any real (non-synthetic) document use. |
| **File security (malware scanning)** | NOT READY | Confirmed absent — `docs/security/DOCUMENT_SECURITY_BOUNDARIES.md:18` and `document-service/src/validation.ts` state only MIME/size/filename allowlist checks; no scanner integration exists. | Required before any real-document pilot. |
| **Event delivery** | NOT READY as a delivery system (write-path only) | `SyntheticOutboxDispatcher` is a manually-invoked function, not a running process; zero external egress code exists anywhere; no retry ceiling or dead-letter handling (DRIFT-12). | Build a real dispatcher/worker only once a named external consumer is authorized — no consumer exists today. |
| **Workers** | NOT PRESENT | No persistent background-process infrastructure exists for any purpose (outbox dispatch, scheduled jobs, etc.) — confirmed via `package.json` scripts (no `dispatch`/`worker` entries). | CONDITIONAL — build only when a specific need is authorized (see CLARITY_TARGET_STATE.md). |
| **Observability (logs/metrics/tracing)** | NOT READY | No pino/winston/opentelemetry/prometheus/datadog/sentry dependency anywhere; only ad hoc `console.log/error/warn` in `api-service/src/{devMain,server}.ts`. | Required before any production deployment; not required for continued development. |
| **Secrets management** | NOT READY (env-file only) | `.env` (git-ignored) holds `DATABASE_URL` only; no secrets-manager integration found. | Required before production; a `.env`-based local flow is appropriate for the current dev-only scope. |
| **Backups** | NOT READY as a real backup mechanism | `scripts/verify-local-migration-recovery.ts` performs a local `pg_dump`/`pg_restore` round-trip against ephemeral, disposable databases it creates and drops itself — a migration-replay smoke test, not a production backup mechanism. | Required before production; not applicable to local dev. |
| **Disaster recovery** | NOT READY | No DR runbook or mechanism beyond the local replay test above. | Required before production. |
| **CI/CD** | PARTIAL | `.github/workflows/ci.yml` runs a single "verify" job (lint, typecheck, root+app tests against an ephemeral Postgres 16 service, `prisma validate`/`generate`/`migrate deploy`, `npm audit --audit-level=high`) — real and required-status-checked on every PR. No deploy/release workflow exists. `pages.yml` publishes only the static `docs/index.html` — not an application deployment. | Required before production: a deploy pipeline. Not required for continued development — the verify gate is solid for that purpose. |
| **Hosting** | NOT READY | No Dockerfile, docker-compose, `fly.toml`, `render.yaml`, `vercel.json`, `Procfile`, or k8s manifest anywhere in the repository; no `docker/` directory at all. | Required before production; a specific hosting decision is an open item (OD-5/OD-6). |
| **External integrations** | NOT APPLICABLE today | No real EHR/payer/clearinghouse/IdP/messaging/BI/ERP/HRIS client exists — only designed ports (e.g. the `IdentityProvider` port). Nothing to evaluate readiness of yet. | FUTURE — build only against a named, authorized integration target. |
| **Security testing** | PARTIAL | `npm audit --audit-level=high` runs in CI and is a required check; no penetration testing, no SAST/DAST tooling, no formal security acceptance process found. | Required before any real-data pilot; see `SECURITY.md`'s "required controls" (already tracked as "Requires security review" in `IMPLEMENTATION_STATUS.md`). |
| **Performance testing** | NOT PRESENT | No load-testing tooling or benchmark harness found for the API or database layer. | CONDITIONAL — required once real-scale traffic is anticipated; not blocking current development. |
| **Pilot controls** | NOT READY | No Studio mutation/publication/feature-flag/worker/deployment controls exist — and per standing house policy (`CLAUDE.md`), none should be added "before server authorization and audit boundaries exist." | Deliberately deferred; not a gap, a boundary. |
| **PHI readiness** | NOT READY, NOT CLAIMED | Synthetic data only, throughout, by explicit repository-wide policy. No claim of HIPAA compliance exists or should be inferred from any status above. | Out of scope until a formal compliance program is authorized — this is a policy boundary, not a backlog item. |
