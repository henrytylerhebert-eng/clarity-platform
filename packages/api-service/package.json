{
  "name": "@clarity/api-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "description": "Thin node:http layer over authentication and command services. Protected routes authenticate bearer tokens through @clarity/auth-service, so tenant and roles come from the database-backed principal rather than request bodies. Exposes auth/session, case decision-rationale, and seven same-organization prescreen routes; synthetic-only by policy and not PHI-ready, deployed, or a production API.",
  "dependencies": {
    "@clarity/auth-service": "*",
    "@clarity/case-repository": "*",
    "@clarity/case-service": "*",
    "@clarity/domain-contracts": "*",
    "@clarity/prescreen-service": "*",
    "zod": "^3.24.1"
  }
}
