{
  "name": "@clarity/prescreen-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "description": "Prescreen command service (Phase 2): six controlled commands behind strict envelopes, explicit synthetic role policy, idempotency, optimistic concurrency, immutable attestation, supplement lineage, and audit/outbox contracts — in-memory gateway only, no persistence, no API, no UI.",
  "dependencies": {
    "@clarity/domain-contracts": "0.1.0",
    "zod": "^3.24.1"
  }
}
