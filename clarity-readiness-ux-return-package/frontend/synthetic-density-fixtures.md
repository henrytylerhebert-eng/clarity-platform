# Synthetic Density Fixtures

The slice does not add canonical fixture records. It creates deterministic queue-scale projections by cycling existing synthetic cases through the local dependency evaluator.

Scenarios:

- 5 maps.
- 25 maps.
- 100 maps.

Test: `app/src/domain/caseDependencyMap.test.ts`.

