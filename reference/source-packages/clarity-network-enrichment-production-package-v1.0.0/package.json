{
  "name": "clarity-network-enrichment-reference",
  "version": "1.0.0-proposed",
  "private": true,
  "description": "Standalone reference implementation for Clarity network enrichment accuracy contracts.",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "test": "npm run clean && npm run build && node --test dist/tests/*.test.js",
    "evaluate": "npm run build && node dist/scripts/evaluate-fixtures.js",
    "check": "npm run test && npm run evaluate"
  }
}
