# Source Packages — Historical Reference Only

> **⚠️ This directory is immutable historical source material. It is NOT the canonical implementation location.**
>
> Do not edit files here. Do not import, build from, or link application code to these paths.
> Canonical documents live under `docs/`. The canonical schema lives at `prisma/schema.prisma`.
> Integration decisions and provenance are recorded in `docs/repository-audit/`.

## Contents

| Path | What it is | Status |
|---|---|---|
| `clarity-ai-master-architecture-v0.2.0-partial/` | The 15 loose files of the Clarity AI Master Architecture package v0.2.0 that were found at the workspace root on 2026-07-10. **The full package (87 files per its own README) was never present locally** — ~72 files, including all 19 numbered directories and `manifest.json`, are missing. | Partial; primary integration source where present |
| `clarity-ai-database-artifact.zip` | Original ZIP as received (2026-07-10 17:30). | Complete |
| `clarity-ai-database-artifact/` | Read-only extraction (9 files: foundation Prisma schema, integration plan, data dictionary, audit rules, seed script, 3 synthetic cases). Its schema is byte-identical to the master package's `schema.foundation.prisma`. | Superseded by inclusion in v0.2.0; retained for provenance |

The earlier Jul 8 package (`clarity-mh-codex-architecture-package.zip`) is preserved with its source documents under `reference/source-documents/` (see the file move map in `docs/repository-audit/06_FILE_MOVE_MAP.md`).
