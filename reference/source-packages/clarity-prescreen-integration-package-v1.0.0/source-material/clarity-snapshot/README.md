# Curated Clarity Snapshot

These files were supplied with the handoff and are copied for implementation context. They are snapshots, not proof of the current live repository state.

The two source images are intentionally not copied. One is a blank/generic referral checklist and one appears to contain patient-level information. Generic structural mappings are in `reference/SOURCE_FORM_MAPPING.md`.

Before implementation, Codex must inspect the live repository and treat current code/tests as the source of truth.
