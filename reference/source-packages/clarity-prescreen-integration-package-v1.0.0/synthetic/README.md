# Synthetic Scenarios

All fixtures are fictional and contain no real patient information. They are designed for deterministic domain, API, UX, and end-to-end tests. The `assessment` object in each scenario conforms to `contracts/prescreen-assessment.schema.json`.
